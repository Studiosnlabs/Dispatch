<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dispatch">

    <application
        android:name=".utils.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Dispatch"
        android:requestLegacyExternalStorage="true"
        android:usesCleartextTraffic="true">
        <activity android:name=".BatchDelivery.Batch_DriversActivity"></activity>
        <activity android:name=".BatchDelivery.Batch_DestinationActivity" />
        <activity android:name=".BatchDelivery.BatchFormActivity" />
        <activity android:name=".RegularDelivery.RegularFormActivity" />
        <activity android:name=".utils.Driver_Login_Activity" />
        <activity android:name=".utils.DriverSignUpActivity" />
        <activity android:name=".utils.UserLoginActivity" />
        <activity android:name=".utils.UserSignUpActivity" />
        <activity android:name=".ExpressDelivery.ExpressAuctionActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity
            android:name=".ExpressDelivery.ExpressDestinationMap"
            android:label="@string/title_activity_express_destination_map" />
        <activity android:name=".ExpressDelivery.ExpressFormActivity" />
        <activity
            android:name=".ExpressUserMapsActivity"
            android:label="@string/title_activity_user_maps" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity android:name=".utils.LoginActivity" />
        <activity android:name=".ExpressDelivery.ExpressActivity" />
        <activity android:name=".Profile.ProfileActivity" />
        <activity android:name=".notifications.NotificationActivity" />
        <activity android:name=".RegularDelivery.RegularActivity" />
        <activity android:name=".BatchDelivery.BatchActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="com.parse.SERVER_URL"
            android:value="@string/back4app_server_url" />
        <meta-data
            android:name="com.parse.APPLICATION_ID"
            android:value="@string/back4app_app_id" />
        <meta-data
            android:name="com.parse.CLIENT_KEY"
            android:value="@string/back4app_client_key" />
    </application>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>


    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

</manifest>